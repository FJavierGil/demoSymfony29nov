{% extends 'base.html.twig' %}

{% block title %}ReferenciaLibro index{% endblock %}

{% block body %}
    <h1>ReferenciaLibro index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Url</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for referencia_libro in referencia_libros %}
            <tr>
                <td>{{ referencia_libro.id }}</td>
                <td>{{ referencia_libro.url }}</td>
                <td>
                    <a href="{{ path('referencia_libro_show', {'id': referencia_libro.id}) }}">show</a>
                    <a href="{{ path('referencia_libro_edit', {'id': referencia_libro.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('referencia_libro_new') }}">Create new</a>
{% endblock %}
