{% extends 'base.html.twig' %}

{% block title %}ReferenciaLibro{% endblock %}

{% block body %}
    <h1>ReferenciaLibro</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ referencia_libro.id }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ referencia_libro.url }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('referencia_libro_index') }}">back to list</a>

    <a href="{{ path('referencia_libro_edit', {'id': referencia_libro.id}) }}">edit</a>

    {{ include('referencia_libro/_delete_form.html.twig') }}
{% endblock %}
